import { CgSpinner } from "react-icons/cg";

export const Spinner = () => {
  return (
    <CgSpinner
      className="infinite"
      style={{
        height: "24px",
        width: "24px",
      }}
    />
  );
};
